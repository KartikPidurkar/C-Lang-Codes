#include<stdio.h>
#include<math.h>
int main(){
    //float x = 3.14;
    float x = 45;

    //x = sqrt(9);
    //x = pow(x,4); x is raised to the power of 4 i.e(2*2*2*2)
    //x = round(x); Normal rounding 3.14=3.00
    //x = ceil(x);  rouding up 3.14=4.00
    //x = floor(x); rounding down 3.14=3.00
    //x = abs(x);  absolute value of the number from zero
    //x = log(x);  log value of number
    //x = sin(x);  Trignometry functions the value of x will be in radians 
    //cos() and tan() and some etc functions 
    // Que] List available trigonometric functions in C and C++ math libraries(AI)
    

    printf("%f",x);




    return 0;

}